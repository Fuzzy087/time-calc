

import re

prompt = str(What is the starting date? (MM-DD-YYYY))
tempvar = sday

def get_non_blank_value(prompt):
    while True:
        $tempvar = input("$prompt")
    if bool(sday)
        print "Error! No blank values allowed"
        continue
    else:
        break
